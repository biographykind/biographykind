/* 1. MAIN CONTAINER RADIUS (GeneratePress Article) */
.separate-containers .inside-article {
    border-radius: 18px !important;
    overflow: hidden;
    border: 1px solid #eef2f6 !important;
    padding: 0 !important; /* This is 0 to merge H1 and Image */
}

/* 2. MERGED HEADER (H1 and Featured Image) */
.entry-header {
    background-color: #f8fafc;
    padding: 30px 25px !important; /* Space inside Title */
    margin: 0 !important;
    border-bottom: 1px solid #e2e8f0;
}

.entry-header h1 {
    margin: 0 !important;
    color: #1a202c;
    /* Font size from your theme panel */
}

.featured-image {
    background-color: #ffffff;
    padding: 20px 25px !important; /* Space around image */
    margin: 0px !important;
    text-align: center;
}

.featured-image img {
    border-radius: 12px !important;
    width: auto !important; /* Respect theme size */
    max-width: 100% !important;
    height: auto !important;
}

/* 3. SECTION BOXES & TOC STANDALONE */
.post-section-box, .bk-toc-wrapper {
    background-color: #ffffff;
    border: 1px solid #e2e8f0;
    border-radius: 12px;
    margin: 0 25px 30px 25px !important; /* Side margin for boxes */
    overflow: hidden;
    box-shadow: 0 4px 10px rgba(0,0,0,0.02);
}

/* 4. PADDING INSIDE BOXES (FIXED MARGIN/PADDING) */
/* Heading Title Bar */
.post-section-box h2, .bk-toc-summary {
    background-color: #f8fafc !important;
    padding: 15px 20px !important;
    margin: 0 !important;
    border-bottom: 1px solid #e2e8f0;
    font-weight: 700;
}

/* Content inside boxes (Paragraphs, etc.) */
.post-section-box > *:not(h2) {
    padding-left: 20px !important;
    padding-right: 20px !important;
    margin-top: 20px !important; /* Space after heading */
    margin-bottom: 20px !important; /* Space between elements */
}

/* 5. INTRO BOX (Directly after Image) */
.post-section-box.intro-section {
    border: none !important;
    box-shadow: none !important;
    margin-top: 0px !important;
    border-bottom: 4px solid #f1f5f9 !important;
    border-bottom-left-radius: 12px;
    border-bottom-right-radius: 12px;
}

/* 6. TOC LIST STYLE */
.bk-toc-summary { display: list-item; cursor: pointer; font-size: 20px; }
.bk-toc-summary::marker { color: #3b82f6; }
.bk-toc-list {
    padding: 20px 20px 20px 45px !important;
    margin: 0 !important;
    list-style: none !important;
}
.bk-toc-list li { position: relative; margin-bottom: 10px; }
.bk-toc-list li::before {
    content: "•"; color: #3b82f6; position: absolute; left: -25px; font-weight: bold; font-size: 1.5rem; top: -5px;
}

/* 7. IMAGE FIX (Respects WP Admin Size) */
.post-section-box img {
    border-radius: 10px !important;
    width: auto !important;
    max-width: 100% !important;
    height: auto !important;
    display: block;
    margin: 20px auto !important;
}

/* 8. FOOTER ELEMENTS (Categories, Related Posts) */
footer.entry-meta, .related-posts, .comments-area {
    padding: 25px !important;
    margin: 20px 25px !important;
    background: #ffffff;
    border-radius: 12px;
    border: 1px solid #eef2f6;
}

/* Mobile Adjustments */
@media (max-width: 768px) {
    .post-section-box, .bk-toc-wrapper, footer.entry-meta, .related-posts {
        margin: 0 10px 25px 10px !important;
    }
    .post-section-box > *:not(h2) { padding-left: 15px !important; padding-right: 15px !important; }
}


/* ============================================================
   SIDEBAR WIDGETS SECTION DESIGN (Matched with Content)
   ============================================================ */

/* 1. Sidebar Widget Container */
.sidebar .widget {
    background-color: #ffffff !important;
    border: 1px solid #e2e8f0 !important;
    border-radius: 12px !important;
    margin-bottom: 30px !important;
    overflow: hidden !important; /* Header background curve ke liye */
    box-shadow: 0 4px 10px rgba(0,0,0,0.02) !important;
    padding: 0 !important; /* Padding title bar ke niche se shuru hogi */
}

/* 2. Sidebar Widget Title (Title Bar Look) */
.sidebar .widget .widget-title {
    background-color: #f8fafc !important; /* Same as Post H2 background */
    color: #1e293b !important;
    padding: 12px 20px !important;
    margin: 0 !important;
    border-bottom: 1px solid #e2e8f0 !important;
    font-weight: 700 !important;
    /* Font size theme panel wala hi rahega */
}

/* 3. Sidebar Widget Content Padding */
/* Isse widget ke andar ka text/links chipkenge nahi */
.sidebar .widget > *:not(.widget-title) {
    padding-left: 20px !important;
    padding-right: 20px !important;
    margin-top: 20px !important;
    margin-bottom: 20px !important;
}

/* 4. Sidebar List Styling (Optional: For Recent Posts, Categories) */
.sidebar .widget ul {
    list-style: none !important;
    margin-left: 0 !important;
    padding-left: 0 !important;
}

.sidebar .widget ul li {
    padding: 8px 0 !important;
    border-bottom: 1px solid #f1f5f9; /* Light separator between links */
}

.sidebar .widget ul li:last-child {
    border-bottom: none !important; /* Last link ke niche line nahi */
}

.sidebar .widget ul li a {
    text-decoration: none;
    color: #334155;
    transition: color 0.2s;
}

.sidebar .widget ul li a:hover {
    color: #3b82f6; /* Link hover color */
}

/* 5. Mobile Adjustment for Sidebar */
@media (max-width: 768px) {
    .sidebar .widget {
        margin-left: 10px !important;
        margin-right: 10px !important;
    }
}

/* 1. Article Images - Max Width 600px */
.post-section-box img {
    border-radius: 8px !important;
    width: auto !important;         /* Preserve aspect ratio */
    max-width: 500px !important;    /* Fixed max width */
    max-height: 500px !important;
    display: block;
    margin: 25px auto !important;   /* Centered */
}

/* 2. Featured Image (Thumbnail) - Respected Theme Size */
.featured-image img {
    max-width: 100% !important;
    width: auto !important; 
    height: auto !important;
    margin: 0 auto !important;
    display: block;
}

/* 3. Mobile Display */
@media (max-width: 768px) {
    .post-section-box img {
        max-width: 100% !important; /* Full width on small screens */
        padding: 0 5px;
    }
}

